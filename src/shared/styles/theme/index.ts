import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#0f334c",
      dark: "#03263a",
      light: "#49769a",
      contrastText: "#ffffff",
    },
    secondary: {
      main: "#ffffff",
    },
  },
});
