import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#49769a",
      dark: "#0f334c",
      light: "#49769a",
      contrastText: "#ffffff",
    },
    secondary: {
      main: "#ffffff",
    },
  },
});
